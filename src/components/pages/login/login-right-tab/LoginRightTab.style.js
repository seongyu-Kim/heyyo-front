import styled from "styled-components";
import { Font } from "@/assets/fonts/Font";

export const LoginRightDiv = styled.div`
  width: 66.4rem;
  height: 59.9rem;
  background: #fff;
  box-shadow: 4px 4px 30px 2px rgba(0, 0, 0, 0.1);
  display: flex;
  flex-direction: column;
  text-align: center;
`;

export const LoginRightTextSpan = styled.span`
  display: block;
  color: #000;
  font-size: ${Font.Size.XL};
  font-weight: 700;
  padding-top: 13.9rem;
  padding-bottom: 4.4rem;
`;
