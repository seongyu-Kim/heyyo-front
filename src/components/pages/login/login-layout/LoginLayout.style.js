import styled from "styled-components";

export const LoginLayout = styled.div`
  position: relative;
  width: 96.5rem;
  height: 59.9rem;
  display: flex;
  top: calc(50vh - 6.6rem - (59.9rem * 0.5));
  left: calc(50vw - (96.4rem * 0.5));
`;
